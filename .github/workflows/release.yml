name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  NYM_REPO: "https://github.com/nymtech/nym"
  NYM_TAG: "nym-binaries-v2026.5-raclette"

jobs:
  build-macos:
    name: macOS (${{ matrix.arch }})
    runs-on: macos-latest
    strategy:
      matrix:
        include:
          - arch: aarch64
            target: aarch64-apple-darwin
          - arch: x86_64
            target: x86_64-apple-darwin
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build Nym socks5-client
        run: |
          git clone --depth 1 --branch ${{ env.NYM_TAG }} ${{ env.NYM_REPO }} nym-src
          cd nym-src
          cargo build --release --target ${{ matrix.target }} -p nym-socks5-client
          mkdir -p ../ui/src-tauri/binaries
          cp target/${{ matrix.target }}/release/nym-socks5-client ../ui/src-tauri/binaries/nym-socks5-client-${{ matrix.target }}
          chmod +x ../ui/src-tauri/binaries/*
          cd ../ui/src-tauri && jq '.bundle.externalBin = ["binaries/nym-socks5-client"]' tauri.conf.json > tmp.json && mv tmp.json tauri.conf.json

      - name: Install frontend dependencies
        run: cd ui && npm install

      - name: Build
        run: cd ui && npm run tauri build -- --target ${{ matrix.target }}

      - name: Upload DMG
        uses: actions/upload-artifact@v4
        with:
          name: nonos-browser-macos-${{ matrix.arch }}
          path: target/${{ matrix.target }}/release/bundle/dmg/*.dmg
          if-no-files-found: warn

  build-linux:
    name: Linux (x86_64)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Setup Nym binary
        run: |
          mkdir -p ui/src-tauri/binaries
          curl -L "https://github.com/nymtech/nym/releases/download/${{ env.NYM_TAG }}/nym-socks5-client" \
            -o ui/src-tauri/binaries/nym-socks5-client-x86_64-unknown-linux-gnu
          chmod +x ui/src-tauri/binaries/*
          cd ui/src-tauri && jq '.bundle.externalBin = ["binaries/nym-socks5-client"]' tauri.conf.json > tmp.json && mv tmp.json tauri.conf.json

      - name: Install frontend dependencies
        run: cd ui && npm install

      - name: Build
        run: cd ui && npm run tauri build

      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: nonos-browser-linux-x86_64
          path: target/release/bundle/appimage/*.AppImage
          if-no-files-found: warn

      - name: Upload Deb
        uses: actions/upload-artifact@v4
        with:
          name: nonos-browser-linux-deb
          path: target/release/bundle/deb/*.deb
          if-no-files-found: warn

  build-windows:
    name: Windows (x86_64)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build Nym socks5-client
        shell: bash
        run: |
          git clone --depth 1 --branch ${{ env.NYM_TAG }} ${{ env.NYM_REPO }} nym-src
          cd nym-src
          cargo build --release -p nym-socks5-client
          mkdir -p ../ui/src-tauri/binaries
          cp target/release/nym-socks5-client.exe ../ui/src-tauri/binaries/nym-socks5-client-x86_64-pc-windows-msvc.exe
          cd ../ui/src-tauri && cat tauri.conf.json | jq '.bundle.externalBin = ["binaries/nym-socks5-client"]' > tmp.json && mv tmp.json tauri.conf.json

      - name: Install frontend dependencies
        run: cd ui && npm install

      - name: Build
        run: cd ui && npm run tauri build

      - name: Upload MSI
        uses: actions/upload-artifact@v4
        with:
          name: nonos-browser-windows-x86_64
          path: target/release/bundle/msi/*.msi
          if-no-files-found: warn

  release:
    name: Create Release
    needs: [build-macos, build-linux, build-windows]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display artifacts
        run: ls -R artifacts || echo "No artifacts directory"
        continue-on-error: true

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*.dmg
            artifacts/**/*.AppImage
            artifacts/**/*.deb
            artifacts/**/*.msi
          body: |
            ## NONOS Browser ${{ github.ref_name }}

            Privacy browser with native Nym mixnet integration.

            ### Downloads
            - **macOS (Apple Silicon)**: `NONOS-Browser_*_aarch64.dmg`
            - **macOS (Intel)**: `NONOS-Browser_*_x64.dmg`
            - **Linux (AppImage)**: `nonos-browser_*.AppImage`
            - **Linux (Deb)**: `nonos-browser_*.deb`
            - **Windows**: `NONOS-Browser_*.msi`

            ### Included
            - Nym mixnet client bundled
            - NOX wallet with Sepolia testnet
            - Staking interface

            ### Requirements
            - macOS 12+ / Ubuntu 22.04+ / Windows 11
          draft: false
          prerelease: false
